<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .navbar {
            background: linear-gradient(135deg, #007bff, #0056b3);
            padding: 1rem 0;
        }
        
        .navbar-brand {
            font-weight: 600;
            font-size: 1.5rem;
        }
        
        .main-content {
            padding: 2rem 0;
        }
        
        .dashboard-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            margin-bottom: 2rem;
        }
        
        .user-card {
            border: 1px solid #e9ecef;
            border-radius: 10px;
            padding: 1rem;
            margin-bottom: 1rem;
            transition: all 0.3s ease;
        }
        
        .user-card:hover {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transform: translateY(-2px);
        }
        
        .btn-logout {
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            color: white;
        }
        
        .btn-logout:hover {
            background: rgba(255, 255, 255, 0.3);
            color: white;
        }
        
        .role-badge {
            font-size: 0.8rem;
            padding: 0.25rem 0.5rem;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-dark">
        <div class="container">
            <a href="/dashboard" class="navbar-brand">
                <i class="fas fa-cloud me-2"></i>
                LXCloud Simple
            </a>
            <div class="navbar-nav flex-row">
                <span class="navbar-text me-3">
                    <i class="fas fa-user me-1"></i>
                    <%= user.name %>
                </span>
                <form method="POST" action="/logout" class="d-inline">
                    <button type="submit" class="btn btn-logout btn-sm">
                        <i class="fas fa-sign-out-alt me-1"></i>
                        Logout
                    </button>
                </form>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container main-content">
        <div class="row">
            <div class="col-md-8">
                <div class="dashboard-card">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h4 class="mb-0">
                            <i class="fas fa-users me-2"></i>
                            User Management
                        </h4>
                        <span class="badge bg-primary">
                            <%= users.length %> Total Users
                        </span>
                    </div>
                    
                    <div class="row">
                        <% users.forEach(function(userItem) { %>
                            <div class="col-md-6">
                                <div class="user-card">
                                    <div class="d-flex justify-content-between align-items-start mb-2">
                                        <div>
                                            <h6 class="mb-1">
                                                <i class="fas fa-user me-1"></i>
                                                <%= userItem.name %>
                                            </h6>
                                            <small class="text-muted">
                                                <i class="fas fa-envelope me-1"></i>
                                                <%= userItem.email %>
                                            </small>
                                        </div>
                                        <span class="badge <%= userItem.role === 'admin' ? 'bg-danger' : 'bg-secondary' %> role-badge">
                                            <%= userItem.role %>
                                        </span>
                                    </div>
                                    
                                    <div class="row text-center mt-3">
                                        <div class="col-6">
                                            <small class="text-muted d-block">ID</small>
                                            <strong><%= userItem.id %></strong>
                                        </div>
                                        <div class="col-6">
                                            <small class="text-muted d-block">Created</small>
                                            <strong>
                                                <%= new Date(userItem.created_at).toLocaleDateString() %>
                                            </strong>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                    
                    <% if (users.length === 0) { %>
                        <div class="text-center py-4">
                            <i class="fas fa-users text-muted" style="font-size: 3rem;"></i>
                            <h5 class="mt-3 text-muted">No users found</h5>
                            <p class="text-muted">There are no users in the system yet.</p>
                        </div>
                    <% } %>
                </div>
            </div>
            
            <div class="col-md-4">
                <div class="dashboard-card">
                    <h5 class="mb-3">
                        <i class="fas fa-cog me-2"></i>
                        User Statistics
                    </h5>
                    
                    <div class="row text-center mb-3">
                        <div class="col-6">
                            <div class="p-2">
                                <div class="h3 text-primary mb-0">
                                    <%= users.filter(u => u.role === 'admin').length %>
                                </div>
                                <small class="text-muted">Admins</small>
                            </div>
                        </div>
                        <div class="col-6">
                            <div class="p-2">
                                <div class="h3 text-success mb-0">
                                    <%= users.filter(u => u.role === 'user').length %>
                                </div>
                                <small class="text-muted">Users</small>
                            </div>
                        </div>
                    </div>
                    
                    <hr>
                    
                    <h6 class="mb-3">
                        <i class="fas fa-info-circle me-2"></i>
                        Information
                    </h6>
                    
                    <div class="small">
                        <div class="mb-2">
                            <strong>Storage:</strong> File-based (JSON)<br>
                            <strong>File:</strong> data/users.json<br>
                            <strong>Encryption:</strong> bcrypt
                        </div>
                        
                        <div class="mb-3">
                            <strong>Default Admin:</strong><br>
                            Email: admin@lxcloud.local<br>
                            Password: admin123
                        </div>
                    </div>
                    
                    <div class="alert alert-info">
                        <i class="fas fa-lightbulb me-2"></i>
                        <strong>Note:</strong> User management is simplified in this version. 
                        Users are stored in a JSON file for easy management.
                    </div>
                </div>
                
                <div class="dashboard-card">
                    <h6 class="mb-3">
                        <i class="fas fa-tools me-2"></i>
                        Quick Actions
                    </h6>
                    
                    <a href="/dashboard" class="btn btn-outline-primary btn-sm w-100 mb-2">
                        <i class="fas fa-arrow-left me-1"></i>
                        Back to Dashboard
                    </a>
                    
                    <button class="btn btn-outline-secondary btn-sm w-100" onclick="refreshUsers()">
                        <i class="fas fa-sync me-1"></i>
                        Refresh Users
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <script>
        function refreshUsers() {
            window.location.reload();
        }
    </script>
</body>
</html>