# Database Configuration
DB_HOST=localhost
DB_USER=lxcloud
DB_PASSWORD=lxcloud123
DB_NAME=lxcloud

# SQLite Fallback (used when MySQL is unavailable)
# SQLITE_FALLBACK_URI=sqlite:///custom_fallback.db

# MQTT
MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_TOPIC_PREFIX=lxcloud

# Security
SECRET_KEY=your-secret-key-here-change-in-production

# Flask
FLASK_ENV=development
FLASK_DEBUG=True